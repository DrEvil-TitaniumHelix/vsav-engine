# Third-party game assets — NEVER commit (Decision Games / SPI property)
*.vmod
*.vsav
*.pdf
*.png
*.jpg
*.bmp
literature/
live/
ref/

# Derived data (regenerate with engine/extract_terrain.py from your own module copy).
# The release games ship their terrain baked in (our own hex-terrain derivation,
# not copyrighted art) so a fresh clone runs and CI can validate them.
terrain.json
!games/afrika-korps-classic-ah/terrain.json
!games/blue-and-gray-chickamauga/terrain.json
!games/westwall-arnhem/terrain.json

# Local working notes
INTERNAL_NOTES.md
PROJECT_HISTORY.md
MODULE_WISHLIST.md
CLAUDE.md
.claude/

# Python
__pycache__/
*.pyc
engine/baseline.json

# PyInstaller intermediates
/build/
*.app/

# SHIPPED IN THE REPO (closed dev+tester distribution — Bruce 2026-07-10):
# games_bundled/ = the release games, self-contained (only the counters the
# scenario uses, no rulebook scans).
# The prebuilt Windows exe is distributed via GitHub Releases + Google Drive,
# NOT committed to git (kept the 47 MB binary out of history).
dist/*
*.exe
!games_bundled/**
/baseline.json

games/*/eval_llm_out/

runs/
